{# THIS IS THE ACTUAL PAGE THAT WILL POP UP AND GENERATE WHEN PROJECT IS CLICKED
ON #} {% extends "layouts/base.njk" %} {% block content %}
<section class="py-12 max-w-4xl mx-auto">
  <h1 class="text-3xl font-bold mb-4">{{ title }}</h1>
  <p class="text-gray-700 mb-8">{{ description }}</p>
  <article class="prose prose-lg">
    {{ content | safe }}
  </article>
</section>
{% endblock %}
